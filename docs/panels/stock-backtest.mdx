---
title: "Backtesting"
description: "Historical return and percent-change visualisation for previously-analysed tickers, so you can see how the panel's past signals would have played out."
---

The **Backtesting** panel (internal id `stock-backtest`) renders a compact list of backtested results — one row per ticker — showing how previous Stock Analysis signals fared over their horizon. It's the longitudinal complement to [Stock Analysis](/panels/stock-analysis)'s point-in-time view.

## What the panel shows

A list of `StockBacktestResult[]` rows — one per symbol. Each row shows the ticker and percent-change / return figures formatted via `fmtPct()`, with positive values rendered in a bullish tint (`#8df0b2`) and negative values in a bearish tint (`#ff8c8c`) via `tone()`.

Source state is tracked — `'live' | 'cached'` — so the panel's data badge shows whether the render came from a fresh fetch or a cached snapshot. Empty result sets render a "No stock backtests available yet." retry state via `showRetrying()`.

Panel id is `stock-backtest`; canonical component is `src/components/StockBacktestPanel.ts`. Title "Backtesting" per the panel's `super({...})` call.

## How you reach it

- **Cmd+K**: type *backtest* or *backtesting*.
- **Availability by variant**: registered and enabled by default in the **full/geopolitical** and **finance** variants — free in both. Not present in the tech, commodity, or happy variants. Source: `'stock-backtest'` entries in `FULL_PANELS` (`:50`) and `FINANCE_PANELS`.

## Data sources

Backtests are produced by a Railway cron that replays previous analysis signals against historical price series. Results are stored in `server/worldmonitor/market/v1/premium-stock-store.ts` (name predates the panel's ungating — the store itself has no plan gate) and fetched via `@/services/stock-backtest`; stale or missing stored rows can be recomputed on demand.

| Method + route | Feeds |
|---|---|
| `GET /api/market/v1/list-stored-stock-backtests` | Stored backtest snapshots rendered first when fresh. |
| `GET /api/market/v1/backtest-stock` | On-demand per-symbol backtest recomputation for missing or stale rows. |

## Refresh cadence

Backtest results update with the premium-stock cron. Freshness is signalled by the data badge (`live` vs `cached`).

## Tier & gating

**Free.** No `premium` field on the panel's constructor or either variant registration, and neither RPC is in `PREMIUM_RPC_PATHS`.

## API reference

- [Markets service](/api/MarketService.openapi.yaml) — schemas and parameters for `list-stored-stock-backtests` and `backtest-stock`.
- Related: [Stock Analysis](/panels/stock-analysis) — per-ticker deep dive that seeds the backtest input.
